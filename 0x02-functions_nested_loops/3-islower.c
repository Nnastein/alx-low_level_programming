#include "main.h"
#include <ctype.h>

/**
 * _islower(int c) - checks lowercase
 *
 * Description - Wo
 *
 * Return: int
 */

int _islower(int c)
{
	if (islower(c) == True)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
