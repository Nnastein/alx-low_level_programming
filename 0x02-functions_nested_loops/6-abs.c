#include "main.h"

/**
 * _abs - prints the absolute value
 * @n: n is a number
 *
 * Return: an integer
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
